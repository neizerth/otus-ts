/**
 * Возвращает идентификатор сессии.
 *
 * @remarks
 * Временная реализация до перехода на UUID.
 *
 * @since 1.0.0
 *
 * @privateRemarks
 * Заменить на UUID v4 в следующем релизе.
 */
export function getSessionId(): string {
  return 'session-123';
}
